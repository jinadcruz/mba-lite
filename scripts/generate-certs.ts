#!/usr/bin/env tsx
/**
 * Stub: Certificate generation script.
 * In production, this would use a PDF generation library (e.g. PDFKit)
 * to create and upload certificates to S3.
 */
console.info('Certificate generation — coming in Phase 3.');
